/*
*
* This file was generated by LLRP Code Generator
* see http://llrp-toolkit.cvs.sourceforge.net/llrp-toolkit
* for more information
* Generated on: Mon Apr 10 16:16:13 CST 2017;
*
*/

/*
 * Copyright 2007 ETH Zurich
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 *
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions
 * and limitations under the License.
 *
 */
package org.llrp.ltk.generated.messages;

import org.apache.log4j.Logger;

import org.jdom.Document;
import org.jdom.Element;
import org.jdom.Namespace;

import org.llrp.ltk.exceptions.InvalidLLRPMessageException;
import org.llrp.ltk.exceptions.MissingParameterException;
import org.llrp.ltk.generated.LLRPConstants;
import org.llrp.ltk.generated.parameters.AirProtocolCapabilities;
import org.llrp.ltk.generated.parameters.CommunicationCapabilities;
import org.llrp.ltk.generated.parameters.GenaralCapabilities;
import org.llrp.ltk.generated.parameters.RfCapabilities;
import org.llrp.ltk.generated.parameters.SpecCapabilities;
import org.llrp.ltk.generated.parameters.Status;
import org.llrp.ltk.types.BitList;
import org.llrp.ltk.types.LLRPBitList;
import org.llrp.ltk.types.LLRPMessage;
import org.llrp.ltk.types.SignedShort;
import org.llrp.ltk.types.UnsignedInteger;
import org.llrp.ltk.types.UnsignedShort;

import java.util.LinkedList;
import java.util.List;


/**
 *

See also

 */
public class GetDeviceCapabilitiesAck extends LLRPMessage {
    public static final SignedShort TYPENUM = new SignedShort(351);
    private static final Logger LOGGER = Logger.getLogger(GetDeviceCapabilitiesAck.class);
    public static final String RESPONSETYPE = "";
    protected Status status;
    protected GenaralCapabilities genaralCapabilities;
    protected CommunicationCapabilities communicationCapabilities;
    protected SpecCapabilities specCapabilities;
    protected RfCapabilities rfCapabilities;
    protected AirProtocolCapabilities airProtocolCapabilities;

    /**
     * empty constructor to create new message
     * with LLRP version set to 1.0 (0x1).
     */
    public GetDeviceCapabilitiesAck() {
        //		setVersion(new BitList(0,0,1));
        setVersion(new BitList(0, 0, 0, 0, 0, 0, 0, 1)); // change by wuwh 
    }

    /**
     * Constructor to create message from binary encoded message
     * calls decodeBinary to decode message.
     * @param list to be decoded
     */
    public GetDeviceCapabilitiesAck(final LLRPBitList list)
        throws InvalidLLRPMessageException {
        decodeBinary(list.toByteArray());
    }

    /**
     * Constructor to create message from binary encoded message
     * calls decodeBinary to decode message.
     * @param byteArray representing message
     */
    public GetDeviceCapabilitiesAck(final byte[] byteArray)
        throws InvalidLLRPMessageException {
        decodeBinary(byteArray);
    }

    /**
    * Constructor to create message from xml encoded message
    * calls decodeXML to decode message.
    * @param document to be decoded
    */
    public GetDeviceCapabilitiesAck(final Document document)
        throws InvalidLLRPMessageException {
        decodeXML(document);
    }

    /**
    * {@inheritDoc}
    */
    protected LLRPBitList encodeBinarySpecific()
        throws InvalidLLRPMessageException {
        LLRPBitList resultBits = new LLRPBitList();

        if (status == null) {
            // single parameter, may not be null
            LOGGER.warn(" status not set");
            throw new InvalidLLRPMessageException(" status not set");
        } else {
            resultBits.append(status.encodeBinary());
        }

        if (genaralCapabilities == null) {
            // optional parameter, may be null
            LOGGER.info(" genaralCapabilities not set");
        } else {
            resultBits.append(genaralCapabilities.encodeBinary());
        }

        if (communicationCapabilities == null) {
            // optional parameter, may be null
            LOGGER.info(" communicationCapabilities not set");
        } else {
            resultBits.append(communicationCapabilities.encodeBinary());
        }

        if (specCapabilities == null) {
            // optional parameter, may be null
            LOGGER.info(" specCapabilities not set");
        } else {
            resultBits.append(specCapabilities.encodeBinary());
        }

        if (rfCapabilities == null) {
            // optional parameter, may be null
            LOGGER.info(" rfCapabilities not set");
        } else {
            resultBits.append(rfCapabilities.encodeBinary());
        }

        if (airProtocolCapabilities == null) {
            // optional parameter, may be null
            LOGGER.info(" airProtocolCapabilities not set");
        } else {
            resultBits.append(airProtocolCapabilities.encodeBinary());
        }

        return resultBits;
    }

    /**
    * {@inheritDoc}
    */
    public Document encodeXML() throws InvalidLLRPMessageException {
        try {
            Namespace ns = Namespace.getNamespace("",
                    LLRPConstants.LLRPNAMESPACE);

            Element root = new Element("GetDeviceCapabilitiesAck", ns);
            //	Element root = new Element("GetDeviceCapabilitiesAck");
            root.addNamespaceDeclaration(Namespace.getNamespace("llrp",
                    LLRPConstants.LLRPNAMESPACE));

            if (version == null) {
                throw new InvalidLLRPMessageException("Version not set");
            } else {
                root.setAttribute("Version", version.toInteger().toString());
            }

            if (messageID == null) {
                throw new InvalidLLRPMessageException("MessageID not set");
            } else {
                root.setAttribute("MessageID", messageID.toString(10));
            }

            //parameters
            if (status == null) {
                LOGGER.info("status not set");
                throw new MissingParameterException("status not set");
            } else {
                root.addContent(status.encodeXML(status.getClass()
                                                       .getSimpleName(), ns));
            }

            if (genaralCapabilities == null) {
                LOGGER.info("genaralCapabilities not set");
            } else {
                root.addContent(genaralCapabilities.encodeXML(
                        genaralCapabilities.getClass().getSimpleName(), ns));
            }

            if (communicationCapabilities == null) {
                LOGGER.info("communicationCapabilities not set");
            } else {
                root.addContent(communicationCapabilities.encodeXML(
                        communicationCapabilities.getClass().getSimpleName(), ns));
            }

            if (specCapabilities == null) {
                LOGGER.info("specCapabilities not set");
            } else {
                root.addContent(specCapabilities.encodeXML(
                        specCapabilities.getClass().getSimpleName(), ns));
            }

            if (rfCapabilities == null) {
                LOGGER.info("rfCapabilities not set");
            } else {
                root.addContent(rfCapabilities.encodeXML(
                        rfCapabilities.getClass().getSimpleName(), ns));
            }

            if (airProtocolCapabilities == null) {
                LOGGER.info("airProtocolCapabilities not set");
            } else {
                root.addContent(airProtocolCapabilities.encodeXML(
                        airProtocolCapabilities.getClass().getSimpleName(), ns));
            }

            Document doc = new Document(root);

            if (isValidXMLMessage(doc, LLRPConstants.LLRPMESSAGESCHEMAPATH)) {
                return doc;
            } else {
                return null;
            }
        } catch (IllegalArgumentException e) {
            throw new InvalidLLRPMessageException(e.getMessage());
        } catch (MissingParameterException e) {
            throw new InvalidLLRPMessageException(e.getMessage());
        }
    }

    /**
    * {@inheritDoc}
    */
    protected void decodeBinarySpecific(LLRPBitList binary)
        throws InvalidLLRPMessageException {
        int position = 0;
        int tempByteLength;
        int tempLength = 0;
        int count;
        SignedShort type;
        int fieldCount;

        // look ahead to see type
        // may be optional or exactly once
        type = null;
        tempByteLength = 0;
        tempLength = 0;

        try {
            // if first bit is one it is a TV Parameter
            if (binary.get(position)) {
                // do not take the first bit as it is always 1
                type = new SignedShort(binary.subList(position + 1, 7));
            } else {
                //type = new SignedShort(binary.subList(position+RESERVEDLENGTH, TYPENUMBERLENGTH));
                //tempByteLength = new UnsignedShort(binary.subList(position+RESERVEDLENGTH+ TYPENUMBERLENGTH, UnsignedShort.length())).toShort();			
                //tempLength=8*tempByteLength;
                type = new SignedShort(binary.subList(position,
                            PARAMETERTYPELENGTH)); //change by wuwh
                tempByteLength = new UnsignedShort(binary.subList(position +
                            PARAMETERTYPELENGTH, UnsignedShort.length())).toShort(); //change by wuwh		
                tempLength = 8 * (tempByteLength + 4); //change by wuwh
            }
        } catch (IllegalArgumentException le) {
            // if an IllegalArgumentException is thrown, list was not long enough so the parameter is missing
            LOGGER.warn(
                "GetDeviceCapabilitiesAck misses non optional parameter of type Status");
            throw new InvalidLLRPMessageException(
                "GetDeviceCapabilitiesAck misses non optional parameter of type Status");
        }

        if (binary.get(position)) {
            // length can statically be determined for TV Parameters
            tempLength = status.length();
        }

        if ((type != null) && type.equals(Status.TYPENUM)) {
            status = new Status(binary.subList(position, tempLength));
            position += tempLength;
            LOGGER.debug(" status is instantiated with Status with length" +
                tempLength);
        } else {
            LOGGER.warn(
                "GetDeviceCapabilitiesAck misses non optional parameter of type Status");
            throw new InvalidLLRPMessageException(
                "GetDeviceCapabilitiesAck misses non optional parameter of type Status");
        }

        // look ahead to see type
        // may be optional or exactly once
        type = null;
        tempByteLength = 0;
        tempLength = 0;

        try {
            // if first bit is one it is a TV Parameter
            if (binary.get(position)) {
                // do not take the first bit as it is always 1
                type = new SignedShort(binary.subList(position + 1, 7));
            } else {
                //type = new SignedShort(binary.subList(position+RESERVEDLENGTH, TYPENUMBERLENGTH));
                //tempByteLength = new UnsignedShort(binary.subList(position+RESERVEDLENGTH+ TYPENUMBERLENGTH, UnsignedShort.length())).toShort();			
                //tempLength=8*tempByteLength;
                type = new SignedShort(binary.subList(position,
                            PARAMETERTYPELENGTH)); //change by wuwh
                tempByteLength = new UnsignedShort(binary.subList(position +
                            PARAMETERTYPELENGTH, UnsignedShort.length())).toShort(); //change by wuwh		
                tempLength = 8 * (tempByteLength + 4); //change by wuwh
            }
        } catch (IllegalArgumentException le) {
            // if an IllegalArgumentException is thrown, list was not long enough so the parameter is missing
            LOGGER.info(
                "GetDeviceCapabilitiesAck misses optional parameter of type GenaralCapabilities");
        }

        if (binary.get(position)) {
            // length can statically be determined for TV Parameters
            tempLength = genaralCapabilities.length();
        }

        if ((type != null) && type.equals(GenaralCapabilities.TYPENUM)) {
            genaralCapabilities = new GenaralCapabilities(binary.subList(
                        position, tempLength));
            position += tempLength;
            LOGGER.debug(
                " genaralCapabilities is instantiated with GenaralCapabilities with length" +
                tempLength);
        } else {
            LOGGER.info(
                "GetDeviceCapabilitiesAck misses optional parameter of type GenaralCapabilities");
        }

        // look ahead to see type
        // may be optional or exactly once
        type = null;
        tempByteLength = 0;
        tempLength = 0;

        try {
            // if first bit is one it is a TV Parameter
            if (binary.get(position)) {
                // do not take the first bit as it is always 1
                type = new SignedShort(binary.subList(position + 1, 7));
            } else {
                //type = new SignedShort(binary.subList(position+RESERVEDLENGTH, TYPENUMBERLENGTH));
                //tempByteLength = new UnsignedShort(binary.subList(position+RESERVEDLENGTH+ TYPENUMBERLENGTH, UnsignedShort.length())).toShort();			
                //tempLength=8*tempByteLength;
                type = new SignedShort(binary.subList(position,
                            PARAMETERTYPELENGTH)); //change by wuwh
                tempByteLength = new UnsignedShort(binary.subList(position +
                            PARAMETERTYPELENGTH, UnsignedShort.length())).toShort(); //change by wuwh		
                tempLength = 8 * (tempByteLength + 4); //change by wuwh
            }
        } catch (IllegalArgumentException le) {
            // if an IllegalArgumentException is thrown, list was not long enough so the parameter is missing
            LOGGER.info(
                "GetDeviceCapabilitiesAck misses optional parameter of type CommunicationCapabilities");
        }

        if (binary.get(position)) {
            // length can statically be determined for TV Parameters
            tempLength = communicationCapabilities.length();
        }

        if ((type != null) && type.equals(CommunicationCapabilities.TYPENUM)) {
            communicationCapabilities = new CommunicationCapabilities(binary.subList(
                        position, tempLength));
            position += tempLength;
            LOGGER.debug(
                " communicationCapabilities is instantiated with CommunicationCapabilities with length" +
                tempLength);
        } else {
            LOGGER.info(
                "GetDeviceCapabilitiesAck misses optional parameter of type CommunicationCapabilities");
        }

        // look ahead to see type
        // may be optional or exactly once
        type = null;
        tempByteLength = 0;
        tempLength = 0;

        try {
            // if first bit is one it is a TV Parameter
            if (binary.get(position)) {
                // do not take the first bit as it is always 1
                type = new SignedShort(binary.subList(position + 1, 7));
            } else {
                //type = new SignedShort(binary.subList(position+RESERVEDLENGTH, TYPENUMBERLENGTH));
                //tempByteLength = new UnsignedShort(binary.subList(position+RESERVEDLENGTH+ TYPENUMBERLENGTH, UnsignedShort.length())).toShort();			
                //tempLength=8*tempByteLength;
                type = new SignedShort(binary.subList(position,
                            PARAMETERTYPELENGTH)); //change by wuwh
                tempByteLength = new UnsignedShort(binary.subList(position +
                            PARAMETERTYPELENGTH, UnsignedShort.length())).toShort(); //change by wuwh		
                tempLength = 8 * (tempByteLength + 4); //change by wuwh
            }
        } catch (IllegalArgumentException le) {
            // if an IllegalArgumentException is thrown, list was not long enough so the parameter is missing
            LOGGER.info(
                "GetDeviceCapabilitiesAck misses optional parameter of type SpecCapabilities");
        }

        if (binary.get(position)) {
            // length can statically be determined for TV Parameters
            tempLength = specCapabilities.length();
        }

        if ((type != null) && type.equals(SpecCapabilities.TYPENUM)) {
            specCapabilities = new SpecCapabilities(binary.subList(position,
                        tempLength));
            position += tempLength;
            LOGGER.debug(
                " specCapabilities is instantiated with SpecCapabilities with length" +
                tempLength);
        } else {
            LOGGER.info(
                "GetDeviceCapabilitiesAck misses optional parameter of type SpecCapabilities");
        }

        // look ahead to see type
        // may be optional or exactly once
        type = null;
        tempByteLength = 0;
        tempLength = 0;

        try {
            // if first bit is one it is a TV Parameter
            if (binary.get(position)) {
                // do not take the first bit as it is always 1
                type = new SignedShort(binary.subList(position + 1, 7));
            } else {
                //type = new SignedShort(binary.subList(position+RESERVEDLENGTH, TYPENUMBERLENGTH));
                //tempByteLength = new UnsignedShort(binary.subList(position+RESERVEDLENGTH+ TYPENUMBERLENGTH, UnsignedShort.length())).toShort();			
                //tempLength=8*tempByteLength;
                type = new SignedShort(binary.subList(position,
                            PARAMETERTYPELENGTH)); //change by wuwh
                tempByteLength = new UnsignedShort(binary.subList(position +
                            PARAMETERTYPELENGTH, UnsignedShort.length())).toShort(); //change by wuwh		
                tempLength = 8 * (tempByteLength + 4); //change by wuwh
            }
        } catch (IllegalArgumentException le) {
            // if an IllegalArgumentException is thrown, list was not long enough so the parameter is missing
            LOGGER.info(
                "GetDeviceCapabilitiesAck misses optional parameter of type RfCapabilities");
        }

        if (binary.get(position)) {
            // length can statically be determined for TV Parameters
            tempLength = rfCapabilities.length();
        }

        if ((type != null) && type.equals(RfCapabilities.TYPENUM)) {
            rfCapabilities = new RfCapabilities(binary.subList(position,
                        tempLength));
            position += tempLength;
            LOGGER.debug(
                " rfCapabilities is instantiated with RfCapabilities with length" +
                tempLength);
        } else {
            LOGGER.info(
                "GetDeviceCapabilitiesAck misses optional parameter of type RfCapabilities");
        }

        // look ahead to see type
        // may be optional or exactly once
        type = null;
        tempByteLength = 0;
        tempLength = 0;

        try {
            // if first bit is one it is a TV Parameter
            if (binary.get(position)) {
                // do not take the first bit as it is always 1
                type = new SignedShort(binary.subList(position + 1, 7));
            } else {
                //type = new SignedShort(binary.subList(position+RESERVEDLENGTH, TYPENUMBERLENGTH));
                //tempByteLength = new UnsignedShort(binary.subList(position+RESERVEDLENGTH+ TYPENUMBERLENGTH, UnsignedShort.length())).toShort();			
                //tempLength=8*tempByteLength;
                type = new SignedShort(binary.subList(position,
                            PARAMETERTYPELENGTH)); //change by wuwh
                tempByteLength = new UnsignedShort(binary.subList(position +
                            PARAMETERTYPELENGTH, UnsignedShort.length())).toShort(); //change by wuwh		
                tempLength = 8 * (tempByteLength + 4); //change by wuwh
            }
        } catch (IllegalArgumentException le) {
            // if an IllegalArgumentException is thrown, list was not long enough so the parameter is missing
            LOGGER.info(
                "GetDeviceCapabilitiesAck misses optional parameter of type AirProtocolCapabilities");
        }

        if (binary.get(position)) {
            // length can statically be determined for TV Parameters
            tempLength = airProtocolCapabilities.length();
        }

        if ((type != null) && type.equals(AirProtocolCapabilities.TYPENUM)) {
            airProtocolCapabilities = new AirProtocolCapabilities(binary.subList(
                        position, tempLength));
            position += tempLength;
            LOGGER.debug(
                " airProtocolCapabilities is instantiated with AirProtocolCapabilities with length" +
                tempLength);
        } else {
            LOGGER.info(
                "GetDeviceCapabilitiesAck misses optional parameter of type AirProtocolCapabilities");
        }
    }

    /**
    * {@inheritDoc}
    */
    public void decodeXML(final Document document)
        throws InvalidLLRPMessageException {
        Element temp = null;

        // child element are always in default LLRP namespace
        Namespace ns = Namespace.getNamespace(LLRPConstants.LLRPNAMESPACE);

        try {
            isValidXMLMessage(document, LLRPConstants.LLRPMESSAGESCHEMAPATH);

            Element root = (Element) document.getRootElement().clone();
            List<Element> tempList = null;

            // the version field is always 3 bit long 
            // if the version attribute is not set in the LTK-XML message,
            // it is set to version 001
            String versionAttribute = root.getAttributeValue("Version");

            if (versionAttribute != null) {
                version = new BitList(8); //change by wuwh 3 to 8
                version.setValue(new Integer(versionAttribute));
            } else {
                version = new BitList(0, 0, 0, 0, 0, 0, 0, 1); //change by wuwh 
            }

            messageID = new UnsignedInteger(root.getAttributeValue("MessageID"));

            //parameter - not choices - no special actions needed
            temp = root.getChild("Status", ns);

            if (temp != null) {
                status = new Status(temp);
                LOGGER.info(
                    "setting parameter status for parameter GetDeviceCapabilitiesAck");
            } else {
                LOGGER.warn(
                    "GetDeviceCapabilitiesAck misses non optional parameter of type status");
                throw new MissingParameterException(
                    "GetDeviceCapabilitiesAck misses non optional parameter of type status");
            }

            root.removeChild("Status", ns);
            //parameter - not choices - no special actions needed
            temp = root.getChild("GenaralCapabilities", ns);

            if (temp != null) {
                genaralCapabilities = new GenaralCapabilities(temp);
                LOGGER.info(
                    "setting parameter genaralCapabilities for parameter GetDeviceCapabilitiesAck");
            } else {
                LOGGER.info(
                    "GetDeviceCapabilitiesAck misses optional parameter of type genaralCapabilities");
            }

            root.removeChild("GenaralCapabilities", ns);
            //parameter - not choices - no special actions needed
            temp = root.getChild("CommunicationCapabilities", ns);

            if (temp != null) {
                communicationCapabilities = new CommunicationCapabilities(temp);
                LOGGER.info(
                    "setting parameter communicationCapabilities for parameter GetDeviceCapabilitiesAck");
            } else {
                LOGGER.info(
                    "GetDeviceCapabilitiesAck misses optional parameter of type communicationCapabilities");
            }

            root.removeChild("CommunicationCapabilities", ns);
            //parameter - not choices - no special actions needed
            temp = root.getChild("SpecCapabilities", ns);

            if (temp != null) {
                specCapabilities = new SpecCapabilities(temp);
                LOGGER.info(
                    "setting parameter specCapabilities for parameter GetDeviceCapabilitiesAck");
            } else {
                LOGGER.info(
                    "GetDeviceCapabilitiesAck misses optional parameter of type specCapabilities");
            }

            root.removeChild("SpecCapabilities", ns);
            //parameter - not choices - no special actions needed
            temp = root.getChild("RfCapabilities", ns);

            if (temp != null) {
                rfCapabilities = new RfCapabilities(temp);
                LOGGER.info(
                    "setting parameter rfCapabilities for parameter GetDeviceCapabilitiesAck");
            } else {
                LOGGER.info(
                    "GetDeviceCapabilitiesAck misses optional parameter of type rfCapabilities");
            }

            root.removeChild("RfCapabilities", ns);
            //parameter - not choices - no special actions needed
            temp = root.getChild("AirProtocolCapabilities", ns);

            if (temp != null) {
                airProtocolCapabilities = new AirProtocolCapabilities(temp);
                LOGGER.info(
                    "setting parameter airProtocolCapabilities for parameter GetDeviceCapabilitiesAck");
            } else {
                LOGGER.info(
                    "GetDeviceCapabilitiesAck misses optional parameter of type airProtocolCapabilities");
            }

            root.removeChild("AirProtocolCapabilities", ns);

            if (root.getChildren().size() > 0) {
                String message = "GetDeviceCapabilitiesAck has unknown element " +
                    ((Element) root.getChildren().get(0)).getName();
                throw new InvalidLLRPMessageException(message);
            }
        } catch (IllegalArgumentException e) {
            throw new InvalidLLRPMessageException(e.getMessage());
        } catch (MissingParameterException e) {
            throw new InvalidLLRPMessageException(e.getMessage());
        }
    }

    //setters

    /**
    * set status of type Status.
    * @param  status to be set
    */
    public void setStatus(final Status status) {
        this.status = status;
    }

    /**
    * set genaralCapabilities of type GenaralCapabilities.
    * @param  genaralCapabilities to be set
    */
    public void setGenaralCapabilities(
        final GenaralCapabilities genaralCapabilities) {
        this.genaralCapabilities = genaralCapabilities;
    }

    /**
    * set communicationCapabilities of type CommunicationCapabilities.
    * @param  communicationCapabilities to be set
    */
    public void setCommunicationCapabilities(
        final CommunicationCapabilities communicationCapabilities) {
        this.communicationCapabilities = communicationCapabilities;
    }

    /**
    * set specCapabilities of type SpecCapabilities.
    * @param  specCapabilities to be set
    */
    public void setSpecCapabilities(final SpecCapabilities specCapabilities) {
        this.specCapabilities = specCapabilities;
    }

    /**
    * set rfCapabilities of type RfCapabilities.
    * @param  rfCapabilities to be set
    */
    public void setRfCapabilities(final RfCapabilities rfCapabilities) {
        this.rfCapabilities = rfCapabilities;
    }

    /**
    * set airProtocolCapabilities of type AirProtocolCapabilities.
    * @param  airProtocolCapabilities to be set
    */
    public void setAirProtocolCapabilities(
        final AirProtocolCapabilities airProtocolCapabilities) {
        this.airProtocolCapabilities = airProtocolCapabilities;
    }

    // end setter

    //getters

    /**
    * get status of type Status .
    * @return  Status
    */
    public Status getStatus() {
        return status;
    }

    /**
    * get genaralCapabilities of type GenaralCapabilities .
    * @return  GenaralCapabilities
    */
    public GenaralCapabilities getGenaralCapabilities() {
        return genaralCapabilities;
    }

    /**
    * get communicationCapabilities of type CommunicationCapabilities .
    * @return  CommunicationCapabilities
    */
    public CommunicationCapabilities getCommunicationCapabilities() {
        return communicationCapabilities;
    }

    /**
    * get specCapabilities of type SpecCapabilities .
    * @return  SpecCapabilities
    */
    public SpecCapabilities getSpecCapabilities() {
        return specCapabilities;
    }

    /**
    * get rfCapabilities of type RfCapabilities .
    * @return  RfCapabilities
    */
    public RfCapabilities getRfCapabilities() {
        return rfCapabilities;
    }

    /**
    * get airProtocolCapabilities of type AirProtocolCapabilities .
    * @return  AirProtocolCapabilities
    */
    public AirProtocolCapabilities getAirProtocolCapabilities() {
        return airProtocolCapabilities;
    }

    // end getters

    //add methods

    // end add
    @Override
    public String getResponseType() {
        return RESPONSETYPE;
    }

    @Override
    public String getName() {
        return "GetDeviceCapabilitiesAck";
    }

    /**
    * return uniuque type number.
    * @return SignedShort holding unique type number
    */
    public SignedShort getTypeNum() {
        return TYPENUM;
    }
}
