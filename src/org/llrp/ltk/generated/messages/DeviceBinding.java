/*
*
* This file was generated by LLRP Code Generator
* see http://llrp-toolkit.cvs.sourceforge.net/llrp-toolkit
* for more information
* Generated on: Mon Apr 10 16:16:14 CST 2017;
*
*/

/*
 * Copyright 2007 ETH Zurich
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 *
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions
 * and limitations under the License.
 *
 */
package org.llrp.ltk.generated.messages;

import org.apache.log4j.Logger;

import org.jdom.Document;
import org.jdom.Element;
import org.jdom.Namespace;

import org.llrp.ltk.exceptions.InvalidLLRPMessageException;
import org.llrp.ltk.exceptions.MissingParameterException;
import org.llrp.ltk.generated.LLRPConstants;
import org.llrp.ltk.generated.enumerations.DeviceBindingType;
import org.llrp.ltk.types.BitList;
import org.llrp.ltk.types.LLRPBitList;
import org.llrp.ltk.types.LLRPMessage;
import org.llrp.ltk.types.SignedShort;
import org.llrp.ltk.types.UnsignedByteArray_HEX;
import org.llrp.ltk.types.UnsignedInteger;
import org.llrp.ltk.types.UnsignedShort;

import java.util.LinkedList;
import java.util.List;


/**
 *

See also

 */
public class DeviceBinding extends LLRPMessage {
    public static final SignedShort TYPENUM = new SignedShort(600);
    private static final Logger LOGGER = Logger.getLogger(DeviceBinding.class);
    public static final String RESPONSETYPE = "DeviceBindingAck";
    protected DeviceBindingType bindingType;
    protected UnsignedByteArray_HEX bindingData;

    /**
     * empty constructor to create new message
     * with LLRP version set to 1.0 (0x1).
     */
    public DeviceBinding() {
        //		setVersion(new BitList(0,0,1));
        setVersion(new BitList(0, 0, 0, 0, 0, 0, 0, 1)); // change by wuwh 
    }

    /**
     * Constructor to create message from binary encoded message
     * calls decodeBinary to decode message.
     * @param list to be decoded
     */
    public DeviceBinding(final LLRPBitList list)
        throws InvalidLLRPMessageException {
        decodeBinary(list.toByteArray());
    }

    /**
     * Constructor to create message from binary encoded message
     * calls decodeBinary to decode message.
     * @param byteArray representing message
     */
    public DeviceBinding(final byte[] byteArray)
        throws InvalidLLRPMessageException {
        decodeBinary(byteArray);
    }

    /**
    * Constructor to create message from xml encoded message
    * calls decodeXML to decode message.
    * @param document to be decoded
    */
    public DeviceBinding(final Document document)
        throws InvalidLLRPMessageException {
        decodeXML(document);
    }

    /**
    * {@inheritDoc}
    */
    protected LLRPBitList encodeBinarySpecific()
        throws InvalidLLRPMessageException {
        LLRPBitList resultBits = new LLRPBitList();

        if (bindingType == null) {
            LOGGER.warn(" bindingType not set");
            throw new InvalidLLRPMessageException(
                " bindingType not set  for Parameter of Type DeviceBinding");
        }

        resultBits.append(bindingType.encodeBinary());

        if (bindingData == null) {
            LOGGER.warn(" bindingData not set");
            throw new InvalidLLRPMessageException(
                " bindingData not set  for Parameter of Type DeviceBinding");
        }

        resultBits.append(bindingData.encodeBinary());

        return resultBits;
    }

    /**
    * {@inheritDoc}
    */
    public Document encodeXML() throws InvalidLLRPMessageException {
        try {
            Namespace ns = Namespace.getNamespace("",
                    LLRPConstants.LLRPNAMESPACE);

            Element root = new Element("DeviceBinding", ns);
            //	Element root = new Element("DeviceBinding");
            root.addNamespaceDeclaration(Namespace.getNamespace("llrp",
                    LLRPConstants.LLRPNAMESPACE));

            if (version == null) {
                throw new InvalidLLRPMessageException("Version not set");
            } else {
                root.setAttribute("Version", version.toInteger().toString());
            }

            if (messageID == null) {
                throw new InvalidLLRPMessageException("MessageID not set");
            } else {
                root.setAttribute("MessageID", messageID.toString(10));
            }

            if (bindingType == null) {
                LOGGER.warn(" bindingType not set");
                throw new MissingParameterException(" bindingType not set");
            } else {
                root.addContent(bindingType.encodeXML("BindingType", ns));
            }

            if (bindingData == null) {
                LOGGER.warn(" bindingData not set");
                throw new MissingParameterException(" bindingData not set");
            } else {
                root.addContent(bindingData.encodeXML("BindingData", ns));
            }

            //parameters
            Document doc = new Document(root);

            if (isValidXMLMessage(doc, LLRPConstants.LLRPMESSAGESCHEMAPATH)) {
                return doc;
            } else {
                return null;
            }
        } catch (IllegalArgumentException e) {
            throw new InvalidLLRPMessageException(e.getMessage());
        } catch (MissingParameterException e) {
            throw new InvalidLLRPMessageException(e.getMessage());
        }
    }

    /**
    * {@inheritDoc}
    */
    protected void decodeBinarySpecific(LLRPBitList binary)
        throws InvalidLLRPMessageException {
        int position = 0;
        int tempByteLength;
        int tempLength = 0;
        int count;
        SignedShort type;
        int fieldCount;
        bindingType = new DeviceBindingType(binary.subList(position,
                    DeviceBindingType.length()));
        position += DeviceBindingType.length();
        // array. first 16 bits indicate length of array
        fieldCount = new UnsignedShort(binary.subList(position,
                    UnsignedShort.length())).toShort();
        tempLength = (UnsignedByteArray_HEX.length() * fieldCount) +
            UnsignedShort.length();
        bindingData = new UnsignedByteArray_HEX(binary.subList(position,
                    tempLength));
        position += tempLength;
        LOGGER.debug("decoding array of type: UnsignedByteArray_HEX with " +
            tempLength + " length");

        //might need padding
        // must always be blocks of 8 bites, if it is a bitlist, this might not be automatically the case
        if ((tempLength % 8) > 0) {
            position += (8 - (tempLength % 8));
            LOGGER.info("padding needed for bindingData ");
        }
    }

    /**
    * {@inheritDoc}
    */
    public void decodeXML(final Document document)
        throws InvalidLLRPMessageException {
        Element temp = null;

        // child element are always in default LLRP namespace
        Namespace ns = Namespace.getNamespace(LLRPConstants.LLRPNAMESPACE);

        try {
            isValidXMLMessage(document, LLRPConstants.LLRPMESSAGESCHEMAPATH);

            Element root = (Element) document.getRootElement().clone();
            List<Element> tempList = null;

            // the version field is always 3 bit long 
            // if the version attribute is not set in the LTK-XML message,
            // it is set to version 001
            String versionAttribute = root.getAttributeValue("Version");

            if (versionAttribute != null) {
                version = new BitList(8); //change by wuwh 3 to 8
                version.setValue(new Integer(versionAttribute));
            } else {
                version = new BitList(0, 0, 0, 0, 0, 0, 0, 1); //change by wuwh 
            }

            messageID = new UnsignedInteger(root.getAttributeValue("MessageID"));
            temp = root.getChild("BindingType", root.getNamespace());

            //check if this element exist
            if (temp != null) {
                bindingType = new DeviceBindingType(temp);
            } else {
                LOGGER.warn(
                    "Element bindingType not provided in xml as child of " +
                    root.getName());
                throw new MissingParameterException(
                    "Element bindingType not provided");
            }

            root.removeChild("BindingType", root.getNamespace());
            temp = root.getChild("BindingData", root.getNamespace());

            //check if this element exist
            if (temp != null) {
                bindingData = new UnsignedByteArray_HEX(temp);
            } else {
                LOGGER.warn(
                    "Element bindingData not provided in xml as child of " +
                    root.getName());
                throw new MissingParameterException(
                    "Element bindingData not provided");
            }

            root.removeChild("BindingData", root.getNamespace());

            if (root.getChildren().size() > 0) {
                String message = "DeviceBinding has unknown element " +
                    ((Element) root.getChildren().get(0)).getName();
                throw new InvalidLLRPMessageException(message);
            }
        } catch (IllegalArgumentException e) {
            throw new InvalidLLRPMessageException(e.getMessage());
        } catch (MissingParameterException e) {
            throw new InvalidLLRPMessageException(e.getMessage());
        }
    }

    //setters
    /**
    * set bindingType of type DeviceBindingType .
    * @param  bindingType to be set
    */
    public void setBindingType(final DeviceBindingType bindingType) {
        this.bindingType = bindingType;
    }

    /**
    * set bindingData of type UnsignedByteArray_HEX .
    * @param  bindingData to be set
    */
    public void setBindingData(final UnsignedByteArray_HEX bindingData) {
        this.bindingData = bindingData;
    }

    // end setter

    //getters
    /**
    * get bindingType of type DeviceBindingType.
    * @return  DeviceBindingType
    */
    public DeviceBindingType getBindingType() {
        return bindingType;
    }

    /**
    * get bindingData of type  UnsignedByteArray_HEX.
    * @return  UnsignedByteArray_HEX
    */
    public UnsignedByteArray_HEX getBindingData() {
        return bindingData;
    }

    // end getters

    //add methods

    // end add
    @Override
    public String getResponseType() {
        return RESPONSETYPE;
    }

    @Override
    public String getName() {
        return "DeviceBinding";
    }

    /**
    * return uniuque type number.
    * @return SignedShort holding unique type number
    */
    public SignedShort getTypeNum() {
        return TYPENUM;
    }
}
