/*
 *
 * This file was generated by LLRP Code Generator
 * see http://llrp-toolkit.cvs.sourceforge.net/llrp-toolkit
 * for more information
 * Generated on: Mon Apr 10 16:16:18 CST 2017;
 *
 */

/*
 * Copyright 2007 ETH Zurich
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 *
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions
 * and limitations under the License.
 *
 */
package org.llrp.ltk.generated.parameters;

import org.apache.log4j.Logger;

import org.jdom.Content;
import org.jdom.Document;
import org.jdom.Element;
import org.jdom.Namespace;

import org.llrp.ltk.exceptions.InvalidLLRPMessageException;
import org.llrp.ltk.exceptions.MissingParameterException;
import org.llrp.ltk.generated.LLRPConstants;
import org.llrp.ltk.types.Bit;
import org.llrp.ltk.types.BitList;
import org.llrp.ltk.types.LLRPBitList;
import org.llrp.ltk.types.LLRPMessage;
import org.llrp.ltk.types.SignedShort;
import org.llrp.ltk.types.TLVParameter;
import org.llrp.ltk.types.TVParameter;
import org.llrp.ltk.types.UnsignedInteger;
import org.llrp.ltk.types.UnsignedShort;

import java.util.LinkedList;
import java.util.List;


/**
 *

See also

 */

/**
 *

See also
      .
 */
public class ReadMode extends TLVParameter {
    public static final SignedShort TYPENUM = new SignedShort(688);
    private static final Logger LOGGER = Logger.getLogger(ReadMode.class);
    protected Bit repeatReadFlag;
    protected BitList reserved0 = new BitList(7);
    protected UnsignedInteger timer;

    /**
     * empty constructor to create new parameter.
     */
    public ReadMode() {
    }

    /**
     * Constructor to create parameter from binary encoded parameter
     * calls decodeBinary to decode parameter.
     * @param list to be decoded
     */
    public ReadMode(LLRPBitList list) {
        decodeBinary(list);
    }

    /**
    * Constructor to create parameter from xml encoded parameter
    * calls decodeXML to decode parameter.
    * @param element to be decoded
    */
    public ReadMode(Element element) throws InvalidLLRPMessageException {
        decodeXML(element);
    }

    /**
    * {@inheritDoc}
    */
    public LLRPBitList encodeBinarySpecific() {
        LLRPBitList resultBits = new LLRPBitList();

        if (repeatReadFlag == null) {
            LOGGER.warn(" repeatReadFlag not set");
            throw new MissingParameterException(
                " repeatReadFlag not set  for Parameter of Type ReadMode");
        }

        resultBits.append(repeatReadFlag.encodeBinary());
        resultBits.append(reserved0.encodeBinary());

        if (timer == null) {
            LOGGER.warn(" timer not set");
            throw new MissingParameterException(
                " timer not set  for Parameter of Type ReadMode");
        }

        resultBits.append(timer.encodeBinary());

        return resultBits;
    }

    /**
    * {@inheritDoc}
    */
    public Content encodeXML(String name, Namespace ns) {
        // element in namespace defined by parent element
        Element element = new Element(name, ns);
        // child element are always in default LLRP namespace
        ns = Namespace.getNamespace("", LLRPConstants.LLRPNAMESPACE);

        if (repeatReadFlag == null) {
            LOGGER.warn(" repeatReadFlag not set");
            throw new MissingParameterException(" repeatReadFlag not set");
        } else {
            element.addContent(repeatReadFlag.encodeXML("RepeatReadFlag", ns));
        }

        //element.addContent(reserved0.encodeXML("reserved",ns));
        if (timer == null) {
            LOGGER.warn(" timer not set");
            throw new MissingParameterException(" timer not set");
        } else {
            element.addContent(timer.encodeXML("Timer", ns));
        }

        //parameters
        return element;
    }

    /**
    * {@inheritDoc}
    */
    protected void decodeBinarySpecific(LLRPBitList binary) {
        int position = 0 + TYPENUMBERLENGTH + PARAMETERTYPELENGTH; //change by wuwh 
        int tempByteLength;
        int tempLength = 0;
        int count;
        SignedShort type;
        int fieldCount;
        repeatReadFlag = new Bit(binary.subList(position, Bit.length()));
        position += Bit.length();
        position += reserved0.length();
        timer = new UnsignedInteger(binary.subList(position,
                    UnsignedInteger.length()));
        position += UnsignedInteger.length();
    }

    /**
    * {@inheritDoc}
    */
    public void decodeXML(Element element) throws InvalidLLRPMessageException {
        List<Element> tempList = null;
        boolean atLeastOnce = false;

        Element temp = null;

        // child element are always in default LLRP namespace
        Namespace ns = Namespace.getNamespace(LLRPConstants.LLRPNAMESPACE);

        temp = element.getChild("RepeatReadFlag", ns);

        if (temp != null) {
            repeatReadFlag = new Bit(temp);
        }

        element.removeChild("RepeatReadFlag", ns);
        temp = element.getChild("Timer", ns);

        if (temp != null) {
            timer = new UnsignedInteger(temp);
        }

        element.removeChild("Timer", ns);

        if (element.getChildren().size() > 0) {
            String message = "ReadMode has unknown element " +
                ((Element) element.getChildren().get(0)).getName();
            throw new InvalidLLRPMessageException(message);
        }
    }

    //setters
    /**
    * set   repeatReadFlag of type Bit .
    * @param   repeatReadFlag to be set
    */
    public void setRepeatReadFlag(final Bit repeatReadFlag) {
        this.repeatReadFlag = repeatReadFlag;
    }

    /**
    * set   timer of type UnsignedInteger .
    * @param   timer to be set
    */
    public void setTimer(final UnsignedInteger timer) {
        this.timer = timer;
    }

    // end setter

    //getters
    /**
    * get   repeatReadFlag of type Bit.
    * @return   type Bit to be set
    */
    public Bit getRepeatReadFlag() {
        return this.repeatReadFlag;
    }

    /**
    * get   timer of type UnsignedInteger.
    * @return   type UnsignedInteger to be set
    */
    public UnsignedInteger getTimer() {
        return this.timer;
    }

    // end getters

    //add methods

    // end add

    /**
    * For TLV Parameter length can not be determined at compile time. This method therefore always returns 0.
    * @return Integer always zero
    */
    public static Integer length() {
        return 0;
    }

    /**
    * {@inheritDoc}
    */
    public SignedShort getTypeNum() {
        return TYPENUM;
    }

    /**
    * {@inheritDoc}
    */
    public String getName() {
        return "ReadMode";
    }

    /**
    * return string representation. All field values but no parameters are included
    * @return String
    */
    public String toString() {
        String result = "ReadMode: ";
        result += ", repeatReadFlag: ";
        result += repeatReadFlag;

        result += ", timer: ";
        result += timer;
        result = result.replaceFirst(", ", "");

        return result;
    }
}
