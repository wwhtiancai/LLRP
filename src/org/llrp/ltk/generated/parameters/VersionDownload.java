/*
 *
 * This file was generated by LLRP Code Generator
 * see http://llrp-toolkit.cvs.sourceforge.net/llrp-toolkit
 * for more information
 * Generated on: Mon Apr 10 16:16:18 CST 2017;
 *
 */

/*
 * Copyright 2007 ETH Zurich
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 *
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions
 * and limitations under the License.
 *
 */
package org.llrp.ltk.generated.parameters;

import org.apache.log4j.Logger;

import org.jdom.Content;
import org.jdom.Document;
import org.jdom.Element;
import org.jdom.Namespace;

import org.llrp.ltk.exceptions.InvalidLLRPMessageException;
import org.llrp.ltk.exceptions.MissingParameterException;
import org.llrp.ltk.generated.LLRPConstants;
import org.llrp.ltk.generated.enumerations.VersionDownloadServerType;
import org.llrp.ltk.generated.parameters.IPAddress;
import org.llrp.ltk.types.LLRPBitList;
import org.llrp.ltk.types.LLRPMessage;
import org.llrp.ltk.types.SignedShort;
import org.llrp.ltk.types.TLVParameter;
import org.llrp.ltk.types.TVParameter;
import org.llrp.ltk.types.UTF8String;
import org.llrp.ltk.types.UnsignedShort;

import java.util.LinkedList;
import java.util.List;


/**
 *

See also

 */

/**
 *

See also
      .
 */
public class VersionDownload extends TLVParameter {
    public static final SignedShort TYPENUM = new SignedShort(701);
    private static final Logger LOGGER = Logger.getLogger(VersionDownload.class);
    protected VersionDownloadServerType serverType;
    protected UTF8String userName;
    protected UTF8String userPass;
    protected UTF8String versionPath;
    protected UnsignedShort serverPort;
    protected IPAddress iPAddress;

    /**
     * empty constructor to create new parameter.
     */
    public VersionDownload() {
    }

    /**
     * Constructor to create parameter from binary encoded parameter
     * calls decodeBinary to decode parameter.
     * @param list to be decoded
     */
    public VersionDownload(LLRPBitList list) {
        decodeBinary(list);
    }

    /**
    * Constructor to create parameter from xml encoded parameter
    * calls decodeXML to decode parameter.
    * @param element to be decoded
    */
    public VersionDownload(Element element) throws InvalidLLRPMessageException {
        decodeXML(element);
    }

    /**
    * {@inheritDoc}
    */
    public LLRPBitList encodeBinarySpecific() {
        LLRPBitList resultBits = new LLRPBitList();

        if (serverType == null) {
            LOGGER.warn(" serverType not set");
            throw new MissingParameterException(
                " serverType not set  for Parameter of Type VersionDownload");
        }

        resultBits.append(serverType.encodeBinary());

        if (userName == null) {
            LOGGER.warn(" userName not set");
            throw new MissingParameterException(
                " userName not set  for Parameter of Type VersionDownload");
        }

        resultBits.append(userName.encodeBinary());

        if (userPass == null) {
            LOGGER.warn(" userPass not set");
            throw new MissingParameterException(
                " userPass not set  for Parameter of Type VersionDownload");
        }

        resultBits.append(userPass.encodeBinary());

        if (versionPath == null) {
            LOGGER.warn(" versionPath not set");
            throw new MissingParameterException(
                " versionPath not set  for Parameter of Type VersionDownload");
        }

        resultBits.append(versionPath.encodeBinary());

        if (serverPort == null) {
            LOGGER.warn(" serverPort not set");
            throw new MissingParameterException(
                " serverPort not set  for Parameter of Type VersionDownload");
        }

        resultBits.append(serverPort.encodeBinary());

        if (iPAddress == null) {
            // single parameter, may not be null
            LOGGER.warn(" iPAddress not set");
            throw new MissingParameterException(" iPAddress not set");
        } else {
            resultBits.append(iPAddress.encodeBinary());
        }

        return resultBits;
    }

    /**
    * {@inheritDoc}
    */
    public Content encodeXML(String name, Namespace ns) {
        // element in namespace defined by parent element
        Element element = new Element(name, ns);
        // child element are always in default LLRP namespace
        ns = Namespace.getNamespace("", LLRPConstants.LLRPNAMESPACE);

        if (serverType == null) {
            LOGGER.warn(" serverType not set");
            throw new MissingParameterException(" serverType not set");
        } else {
            element.addContent(serverType.encodeXML("ServerType", ns));
        }

        if (userName == null) {
            LOGGER.warn(" userName not set");
            throw new MissingParameterException(" userName not set");
        } else {
            element.addContent(userName.encodeXML("UserName", ns));
        }

        if (userPass == null) {
            LOGGER.warn(" userPass not set");
            throw new MissingParameterException(" userPass not set");
        } else {
            element.addContent(userPass.encodeXML("UserPass", ns));
        }

        if (versionPath == null) {
            LOGGER.warn(" versionPath not set");
            throw new MissingParameterException(" versionPath not set");
        } else {
            element.addContent(versionPath.encodeXML("VersionPath", ns));
        }

        if (serverPort == null) {
            LOGGER.warn(" serverPort not set");
            throw new MissingParameterException(" serverPort not set");
        } else {
            element.addContent(serverPort.encodeXML("ServerPort", ns));
        }

        //parameters
        if (iPAddress == null) {
            LOGGER.info("iPAddress not set");
            throw new MissingParameterException("iPAddress not set");
        } else {
            element.addContent(iPAddress.encodeXML(iPAddress.getClass()
                                                            .getSimpleName(), ns));
        }

        return element;
    }

    /**
    * {@inheritDoc}
    */
    protected void decodeBinarySpecific(LLRPBitList binary) {
        int position = 0 + TYPENUMBERLENGTH + PARAMETERTYPELENGTH; //change by wuwh 
        int tempByteLength;
        int tempLength = 0;
        int count;
        SignedShort type;
        int fieldCount;
        serverType = new VersionDownloadServerType(binary.subList(position,
                    VersionDownloadServerType.length()));
        position += VersionDownloadServerType.length();
        // array. first 16 bits indicate length of array
        fieldCount = new UnsignedShort(binary.subList(position,
                    UnsignedShort.length())).toShort();
        tempLength = (UTF8String.length() * fieldCount) +
            UnsignedShort.length();
        userName = new UTF8String(binary.subList(position, tempLength));
        position += tempLength;
        LOGGER.debug("decoding array of type: UTF8String with " + tempLength +
            " length");

        //might need padding
        // must always be blocks of 8 bites, if it is a bitlist, this might not be automatically the case
        if ((tempLength % 8) > 0) {
            position += (8 - (tempLength % 8));
            LOGGER.info("padding needed for userName ");
        }

        // array. first 16 bits indicate length of array
        fieldCount = new UnsignedShort(binary.subList(position,
                    UnsignedShort.length())).toShort();
        tempLength = (UTF8String.length() * fieldCount) +
            UnsignedShort.length();
        userPass = new UTF8String(binary.subList(position, tempLength));
        position += tempLength;
        LOGGER.debug("decoding array of type: UTF8String with " + tempLength +
            " length");

        //might need padding
        // must always be blocks of 8 bites, if it is a bitlist, this might not be automatically the case
        if ((tempLength % 8) > 0) {
            position += (8 - (tempLength % 8));
            LOGGER.info("padding needed for userPass ");
        }

        // array. first 16 bits indicate length of array
        fieldCount = new UnsignedShort(binary.subList(position,
                    UnsignedShort.length())).toShort();
        tempLength = (UTF8String.length() * fieldCount) +
            UnsignedShort.length();
        versionPath = new UTF8String(binary.subList(position, tempLength));
        position += tempLength;
        LOGGER.debug("decoding array of type: UTF8String with " + tempLength +
            " length");

        //might need padding
        // must always be blocks of 8 bites, if it is a bitlist, this might not be automatically the case
        if ((tempLength % 8) > 0) {
            position += (8 - (tempLength % 8));
            LOGGER.info("padding needed for versionPath ");
        }

        serverPort = new UnsignedShort(binary.subList(position,
                    UnsignedShort.length()));
        position += UnsignedShort.length();

        // look ahead to see type
        // may be optional or exactly once
        type = null;
        tempByteLength = 0;
        tempLength = 0;

        try {
            // if first bit is one it is a TV Parameter
            if (binary.get(position)) {
                // do not take the first bit as it is always 1
                type = new SignedShort(binary.subList(position + 1, 7));
            } else {
                type = new SignedShort(binary.subList(position,
                            PARAMETERTYPELENGTH)); //change by wuwh
                tempByteLength = new UnsignedShort(binary.subList(position +
                            PARAMETERTYPELENGTH, UnsignedShort.length())).toShort(); //change by wuwh	
                tempLength = 8 * (tempByteLength + 4); //change by wuwh
            }
        } catch (IllegalArgumentException le) {
            // if an IllegalArgumentException is thrown, list was not long enough so the parameter is missing
            LOGGER.warn(
                "VersionDownload misses non optional parameter of type IPAddress");
            throw new MissingParameterException(
                "VersionDownload misses non optional parameter of type IPAddress");
        }

        if (binary.get(position)) {
            // length can statically be determined for TV Parameters
            tempLength = iPAddress.length();
        }

        if ((type != null) && type.equals(IPAddress.TYPENUM)) {
            iPAddress = new IPAddress(binary.subList(position, tempLength));
            position += tempLength;
            LOGGER.debug(
                " iPAddress is instantiated with IPAddress with length" +
                tempLength);
        } else {
            LOGGER.warn(
                "VersionDownload misses non optional parameter of type IPAddress");
            throw new MissingParameterException(
                "VersionDownload misses non optional parameter of type IPAddress");
        }
    }

    /**
    * {@inheritDoc}
    */
    public void decodeXML(Element element) throws InvalidLLRPMessageException {
        List<Element> tempList = null;
        boolean atLeastOnce = false;

        Element temp = null;

        // child element are always in default LLRP namespace
        Namespace ns = Namespace.getNamespace(LLRPConstants.LLRPNAMESPACE);

        temp = element.getChild("ServerType", ns);

        if (temp != null) {
            serverType = new VersionDownloadServerType(temp);
        }

        element.removeChild("ServerType", ns);
        temp = element.getChild("UserName", ns);

        if (temp != null) {
            userName = new UTF8String(temp);
        }

        element.removeChild("UserName", ns);
        temp = element.getChild("UserPass", ns);

        if (temp != null) {
            userPass = new UTF8String(temp);
        }

        element.removeChild("UserPass", ns);
        temp = element.getChild("VersionPath", ns);

        if (temp != null) {
            versionPath = new UTF8String(temp);
        }

        element.removeChild("VersionPath", ns);
        temp = element.getChild("ServerPort", ns);

        if (temp != null) {
            serverPort = new UnsignedShort(temp);
        }

        element.removeChild("ServerPort", ns);

        //parameter - not choices - no special actions needed
        temp = element.getChild("IPAddress", ns);

        if (temp != null) {
            iPAddress = new IPAddress(temp);
            LOGGER.info(
                "setting parameter iPAddress for parameter VersionDownload");
        }

        if (temp == null) {
            LOGGER.warn(
                "VersionDownload misses non optional parameter of type iPAddress");
            throw new MissingParameterException(
                "VersionDownload misses non optional parameter of type iPAddress");
        }

        element.removeChild("IPAddress", ns);

        if (element.getChildren().size() > 0) {
            String message = "VersionDownload has unknown element " +
                ((Element) element.getChildren().get(0)).getName();
            throw new InvalidLLRPMessageException(message);
        }
    }

    //setters
    /**
    * set serverType of type VersionDownloadServerType .
    * @param  serverType to be set
    */
    public void setServerType(final VersionDownloadServerType serverType) {
        this.serverType = serverType;
    }

    /**
    * set   userName of type UTF8String .
    * @param   userName to be set
    */
    public void setUserName(final UTF8String userName) {
        this.userName = userName;
    }

    /**
    * set   userPass of type UTF8String .
    * @param   userPass to be set
    */
    public void setUserPass(final UTF8String userPass) {
        this.userPass = userPass;
    }

    /**
    * set   versionPath of type UTF8String .
    * @param   versionPath to be set
    */
    public void setVersionPath(final UTF8String versionPath) {
        this.versionPath = versionPath;
    }

    /**
    * set   serverPort of type UnsignedShort .
    * @param   serverPort to be set
    */
    public void setServerPort(final UnsignedShort serverPort) {
        this.serverPort = serverPort;
    }

    /**
    * set iPAddress of type IPAddress.
    * @param  iPAddress to be set
    */
    public void setIPAddress(final IPAddress iPAddress) {
        this.iPAddress = iPAddress;
    }

    // end setter

    //getters
    /**
    * get serverType of type VersionDownloadServerType.
    * @return  VersionDownloadServerType
    */
    public VersionDownloadServerType getServerType() {
        return serverType;
    }

    /**
    * get   userName of type UTF8String.
    * @return   type UTF8String to be set
    */
    public UTF8String getUserName() {
        return this.userName;
    }

    /**
    * get   userPass of type UTF8String.
    * @return   type UTF8String to be set
    */
    public UTF8String getUserPass() {
        return this.userPass;
    }

    /**
    * get   versionPath of type UTF8String.
    * @return   type UTF8String to be set
    */
    public UTF8String getVersionPath() {
        return this.versionPath;
    }

    /**
    * get   serverPort of type UnsignedShort.
    * @return   type UnsignedShort to be set
    */
    public UnsignedShort getServerPort() {
        return this.serverPort;
    }

    /**
    * get iPAddress of type IPAddress .
    * @return  IPAddress
    */
    public IPAddress getIPAddress() {
        return iPAddress;
    }

    // end getters

    //add methods

    // end add

    /**
    * For TLV Parameter length can not be determined at compile time. This method therefore always returns 0.
    * @return Integer always zero
    */
    public static Integer length() {
        return 0;
    }

    /**
    * {@inheritDoc}
    */
    public SignedShort getTypeNum() {
        return TYPENUM;
    }

    /**
    * {@inheritDoc}
    */
    public String getName() {
        return "VersionDownload";
    }

    /**
    * return string representation. All field values but no parameters are included
    * @return String
    */
    public String toString() {
        String result = "VersionDownload: ";
        result += ", serverType: ";
        result += serverType;
        result += ", userName: ";
        result += userName;
        result += ", userPass: ";
        result += userPass;
        result += ", versionPath: ";
        result += versionPath;
        result += ", serverPort: ";
        result += serverPort;
        result = result.replaceFirst(", ", "");

        return result;
    }
}
