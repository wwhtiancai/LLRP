/*
 *
 * This file was generated by LLRP Code Generator
 * see http://llrp-toolkit.cvs.sourceforge.net/llrp-toolkit
 * for more information
 * Generated on: Mon Apr 10 16:16:15 CST 2017;
 *
 */

/*
 * Copyright 2007 ETH Zurich
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 *
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions
 * and limitations under the License.
 *
 */
package org.llrp.ltk.generated.parameters;

import org.apache.log4j.Logger;

import org.jdom.Content;
import org.jdom.Document;
import org.jdom.Element;
import org.jdom.Namespace;

import org.llrp.ltk.exceptions.InvalidLLRPMessageException;
import org.llrp.ltk.exceptions.MissingParameterException;
import org.llrp.ltk.generated.LLRPConstants;
import org.llrp.ltk.generated.parameters.FrequencyTable;
import org.llrp.ltk.types.Bit;
import org.llrp.ltk.types.BitList;
import org.llrp.ltk.types.LLRPBitList;
import org.llrp.ltk.types.LLRPMessage;
import org.llrp.ltk.types.SignedShort;
import org.llrp.ltk.types.TLVParameter;
import org.llrp.ltk.types.TVParameter;
import org.llrp.ltk.types.UnsignedShort;

import java.util.LinkedList;
import java.util.List;


/**
 *

See also

 */

/**
 *

See also
      .
 */
public class FrequencyInformation extends TLVParameter {
    public static final SignedShort TYPENUM = new SignedShort(359);
    private static final Logger LOGGER = Logger.getLogger(FrequencyInformation.class);
    protected Bit hopping;
    protected BitList reserved0 = new BitList(7);
    protected List<FrequencyTable> frequencyTableList = new LinkedList<FrequencyTable>();

    /**
     * empty constructor to create new parameter.
     */
    public FrequencyInformation() {
    }

    /**
     * Constructor to create parameter from binary encoded parameter
     * calls decodeBinary to decode parameter.
     * @param list to be decoded
     */
    public FrequencyInformation(LLRPBitList list) {
        decodeBinary(list);
    }

    /**
    * Constructor to create parameter from xml encoded parameter
    * calls decodeXML to decode parameter.
    * @param element to be decoded
    */
    public FrequencyInformation(Element element)
        throws InvalidLLRPMessageException {
        decodeXML(element);
    }

    /**
    * {@inheritDoc}
    */
    public LLRPBitList encodeBinarySpecific() {
        LLRPBitList resultBits = new LLRPBitList();

        if (hopping == null) {
            LOGGER.warn(" hopping not set");
            throw new MissingParameterException(
                " hopping not set  for Parameter of Type FrequencyInformation");
        }

        resultBits.append(hopping.encodeBinary());
        resultBits.append(reserved0.encodeBinary());

        if (frequencyTableList == null) {
            LOGGER.warn(" frequencyTableList not set");

            //parameter has to be set - throw exception
            throw new MissingParameterException(" frequencyTableList not set");
        } else {
            for (FrequencyTable field : frequencyTableList) {
                resultBits.append(field.encodeBinary());
            }
        }

        return resultBits;
    }

    /**
    * {@inheritDoc}
    */
    public Content encodeXML(String name, Namespace ns) {
        // element in namespace defined by parent element
        Element element = new Element(name, ns);
        // child element are always in default LLRP namespace
        ns = Namespace.getNamespace("", LLRPConstants.LLRPNAMESPACE);

        if (hopping == null) {
            LOGGER.warn(" hopping not set");
            throw new MissingParameterException(" hopping not set");
        } else {
            element.addContent(hopping.encodeXML("Hopping", ns));
        }

        //element.addContent(reserved0.encodeXML("reserved",ns));
        //parameters
        if (frequencyTableList == null) {
            LOGGER.warn(" frequencyTableList not set");
            throw new MissingParameterException("  frequencyTableList not set");
        }

        for (FrequencyTable field : frequencyTableList) {
            element.addContent(field.encodeXML(field.getClass().getName()
                                                    .replaceAll(field.getClass()
                                                                     .getPackage()
                                                                     .getName() +
                        ".", ""), ns));
        }

        return element;
    }

    /**
    * {@inheritDoc}
    */
    protected void decodeBinarySpecific(LLRPBitList binary) {
        int position = 0 + TYPENUMBERLENGTH + PARAMETERTYPELENGTH; //change by wuwh 
        int tempByteLength;
        int tempLength = 0;
        int count;
        SignedShort type;
        int fieldCount;
        hopping = new Bit(binary.subList(position, Bit.length()));
        position += Bit.length();
        position += reserved0.length();

        // list of parameters
        frequencyTableList = new LinkedList<FrequencyTable>();
        LOGGER.debug("decoding parameter frequencyTableList ");

        while (position < binary.length()) {
            // store if one parameter matched
            boolean atLeastOnce = false;

            // look ahead to see type
            if (binary.get(position)) {
                // do not take the first bit as it is always 1
                type = new SignedShort(binary.subList(position + 1, 7));
            } else {
                type = new SignedShort(binary.subList(position,
                            PARAMETERTYPELENGTH)); //change by wuwh
                tempByteLength = new UnsignedShort(binary.subList(position +
                            PARAMETERTYPELENGTH, UnsignedShort.length())).toShort(); //change by wuwh	
                tempLength = 8 * (tempByteLength + 4); //change by wuwh
            }

            //add parameter to list if type number matches
            if ((type != null) && type.equals(FrequencyTable.TYPENUM)) {
                if (binary.get(position)) {
                    // length can statically be determined for TV Parameters
                    tempLength = FrequencyTable.length();
                }

                frequencyTableList.add(new FrequencyTable(binary.subList(
                            position, tempLength)));
                LOGGER.debug("adding FrequencyTable to frequencyTableList ");
                atLeastOnce = true;
                position += tempLength;
            }

            if (!atLeastOnce) {
                //no parameter matched therefore we jump out of the loop
                break;
            }
        }

        //if list is still empty no parameter matched
        if (frequencyTableList.isEmpty()) {
            LOGGER.warn(
                "encoded message does not contain parameter for non optional frequencyTableList");
            throw new MissingParameterException(
                "FrequencyInformation misses non optional parameter of type FrequencyTable");
        }
    }

    /**
    * {@inheritDoc}
    */
    public void decodeXML(Element element) throws InvalidLLRPMessageException {
        List<Element> tempList = null;
        boolean atLeastOnce = false;

        Element temp = null;

        // child element are always in default LLRP namespace
        Namespace ns = Namespace.getNamespace(LLRPConstants.LLRPNAMESPACE);

        temp = element.getChild("Hopping", ns);

        if (temp != null) {
            hopping = new Bit(temp);
        }

        element.removeChild("Hopping", ns);

        //parameter - not choices - no special actions needed
        //we expect a list of parameters
        frequencyTableList = new LinkedList<FrequencyTable>();
        tempList = element.getChildren("FrequencyTable", ns);

        if ((tempList == null) || tempList.isEmpty()) {
            LOGGER.warn(
                "FrequencyInformation misses non optional parameter of type frequencyTableList");
            throw new MissingParameterException(
                "FrequencyInformation misses non optional parameter of type frequencyTableList");
        } else {
            for (Element e : tempList) {
                frequencyTableList.add(new FrequencyTable(e));
                LOGGER.debug("adding FrequencyTable to frequencyTableList ");
            }
        }

        element.removeChildren("FrequencyTable", ns);

        if (element.getChildren().size() > 0) {
            String message = "FrequencyInformation has unknown element " +
                ((Element) element.getChildren().get(0)).getName();
            throw new InvalidLLRPMessageException(message);
        }
    }

    //setters
    /**
    * set   hopping of type Bit .
    * @param   hopping to be set
    */
    public void setHopping(final Bit hopping) {
        this.hopping = hopping;
    }

    /**
    * set frequencyTableList of type  List &lt;FrequencyTable>.
    * @param  frequencyTableList to be set
    */
    public void setFrequencyTableList(
        final List<FrequencyTable> frequencyTableList) {
        this.frequencyTableList = frequencyTableList;
    }

    // end setter

    //getters
    /**
    * get   hopping of type Bit.
    * @return   type Bit to be set
    */
    public Bit getHopping() {
        return this.hopping;
    }

    /**
    * get frequencyTableList of type List &lt;FrequencyTable> .
    * @return  List &lt;FrequencyTable>
    */
    public List<FrequencyTable> getFrequencyTableList() {
        return frequencyTableList;
    }

    // end getters

    //add methods

    /**
    * add element frequencyTable of type FrequencyTable .
    * @param  frequencyTable of type FrequencyTable
    */
    public void addToFrequencyTableList(FrequencyTable frequencyTable) {
        if (this.frequencyTableList == null) {
            this.frequencyTableList = new LinkedList<FrequencyTable>();
        }

        this.frequencyTableList.add(frequencyTable);
    }

    // end add

    /**
    * For TLV Parameter length can not be determined at compile time. This method therefore always returns 0.
    * @return Integer always zero
    */
    public static Integer length() {
        return 0;
    }

    /**
    * {@inheritDoc}
    */
    public SignedShort getTypeNum() {
        return TYPENUM;
    }

    /**
    * {@inheritDoc}
    */
    public String getName() {
        return "FrequencyInformation";
    }

    /**
    * return string representation. All field values but no parameters are included
    * @return String
    */
    public String toString() {
        String result = "FrequencyInformation: ";
        result += ", hopping: ";
        result += hopping;

        result = result.replaceFirst(", ", "");

        return result;
    }
}
