/*
 *
 * This file was generated by LLRP Code Generator
 * see http://llrp-toolkit.cvs.sourceforge.net/llrp-toolkit
 * for more information
 * Generated on: Mon Apr 10 16:16:17 CST 2017;
 *
 */

/*
 * Copyright 2007 ETH Zurich
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 *
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions
 * and limitations under the License.
 *
 */
package org.llrp.ltk.generated.parameters;

import org.apache.log4j.Logger;

import org.jdom.Content;
import org.jdom.Document;
import org.jdom.Element;
import org.jdom.Namespace;

import org.llrp.ltk.exceptions.InvalidLLRPMessageException;
import org.llrp.ltk.exceptions.MissingParameterException;
import org.llrp.ltk.generated.LLRPConstants;
import org.llrp.ltk.generated.interfaces.AccessSpecResult;
import org.llrp.ltk.generated.parameters.ReadDataInfo;
import org.llrp.ltk.types.LLRPBitList;
import org.llrp.ltk.types.LLRPMessage;
import org.llrp.ltk.types.SignedShort;
import org.llrp.ltk.types.TLVParameter;
import org.llrp.ltk.types.TVParameter;
import org.llrp.ltk.types.UnsignedByte;
import org.llrp.ltk.types.UnsignedShort;

import java.util.LinkedList;
import java.util.List;


/**
 *

See also

 */

/**
 *

See also
      .
 */
public class HbCustomizedReadSpecResult extends TLVParameter
    implements AccessSpecResult {
    public static final SignedShort TYPENUM = new SignedShort(515);
    private static final Logger LOGGER = Logger.getLogger(HbCustomizedReadSpecResult.class);
    protected UnsignedByte result;
    protected UnsignedShort opSpecID;
    protected ReadDataInfo readDataInfo;

    /**
     * empty constructor to create new parameter.
     */
    public HbCustomizedReadSpecResult() {
    }

    /**
     * Constructor to create parameter from binary encoded parameter
     * calls decodeBinary to decode parameter.
     * @param list to be decoded
     */
    public HbCustomizedReadSpecResult(LLRPBitList list) {
        decodeBinary(list);
    }

    /**
    * Constructor to create parameter from xml encoded parameter
    * calls decodeXML to decode parameter.
    * @param element to be decoded
    */
    public HbCustomizedReadSpecResult(Element element)
        throws InvalidLLRPMessageException {
        decodeXML(element);
    }

    /**
    * {@inheritDoc}
    */
    public LLRPBitList encodeBinarySpecific() {
        LLRPBitList resultBits = new LLRPBitList();

        if (result == null) {
            LOGGER.warn(" result not set");
            throw new MissingParameterException(
                " result not set  for Parameter of Type HbCustomizedReadSpecResult");
        }

        resultBits.append(result.encodeBinary());

        if (opSpecID == null) {
            LOGGER.warn(" opSpecID not set");
            throw new MissingParameterException(
                " opSpecID not set  for Parameter of Type HbCustomizedReadSpecResult");
        }

        resultBits.append(opSpecID.encodeBinary());

        if (readDataInfo == null) {
            // optional parameter, may be null
            LOGGER.info(" readDataInfo not set");
        } else {
            resultBits.append(readDataInfo.encodeBinary());
        }

        return resultBits;
    }

    /**
    * {@inheritDoc}
    */
    public Content encodeXML(String name, Namespace ns) {
        // element in namespace defined by parent element
        Element element = new Element(name, ns);
        // child element are always in default LLRP namespace
        ns = Namespace.getNamespace("", LLRPConstants.LLRPNAMESPACE);

        if (result == null) {
            LOGGER.warn(" result not set");
            throw new MissingParameterException(" result not set");
        } else {
            element.addContent(result.encodeXML("Result", ns));
        }

        if (opSpecID == null) {
            LOGGER.warn(" opSpecID not set");
            throw new MissingParameterException(" opSpecID not set");
        } else {
            element.addContent(opSpecID.encodeXML("OpSpecID", ns));
        }

        //parameters
        if (readDataInfo == null) {
            LOGGER.info("readDataInfo not set");
        } else {
            element.addContent(readDataInfo.encodeXML(
                    readDataInfo.getClass().getSimpleName(), ns));
        }

        return element;
    }

    /**
    * {@inheritDoc}
    */
    protected void decodeBinarySpecific(LLRPBitList binary) {
        int position = 0 + TYPENUMBERLENGTH + PARAMETERTYPELENGTH; //change by wuwh 
        int tempByteLength;
        int tempLength = 0;
        int count;
        SignedShort type;
        int fieldCount;
        result = new UnsignedByte(binary.subList(position, UnsignedByte.length()));
        position += UnsignedByte.length();
        opSpecID = new UnsignedShort(binary.subList(position,
                    UnsignedShort.length()));
        position += UnsignedShort.length();

        // look ahead to see type
        // may be optional or exactly once
        type = null;
        tempByteLength = 0;
        tempLength = 0;

        try {
            // if first bit is one it is a TV Parameter
            if (binary.get(position)) {
                // do not take the first bit as it is always 1
                type = new SignedShort(binary.subList(position + 1, 7));
            } else {
                type = new SignedShort(binary.subList(position,
                            PARAMETERTYPELENGTH)); //change by wuwh
                tempByteLength = new UnsignedShort(binary.subList(position +
                            PARAMETERTYPELENGTH, UnsignedShort.length())).toShort(); //change by wuwh	
                tempLength = 8 * (tempByteLength + 4); //change by wuwh
            }
        } catch (IllegalArgumentException le) {
            // if an IllegalArgumentException is thrown, list was not long enough so the parameter is missing
            LOGGER.info(
                "HbCustomizedReadSpecResult misses optional parameter of type ReadDataInfo");
        }

        if (binary.get(position)) {
            // length can statically be determined for TV Parameters
            tempLength = readDataInfo.length();
        }

        if ((type != null) && type.equals(ReadDataInfo.TYPENUM)) {
            readDataInfo = new ReadDataInfo(binary.subList(position, tempLength));
            position += tempLength;
            LOGGER.debug(
                " readDataInfo is instantiated with ReadDataInfo with length" +
                tempLength);
        } else {
            LOGGER.info(
                "HbCustomizedReadSpecResult misses optional parameter of type ReadDataInfo");
        }
    }

    /**
    * {@inheritDoc}
    */
    public void decodeXML(Element element) throws InvalidLLRPMessageException {
        List<Element> tempList = null;
        boolean atLeastOnce = false;

        Element temp = null;

        // child element are always in default LLRP namespace
        Namespace ns = Namespace.getNamespace(LLRPConstants.LLRPNAMESPACE);

        temp = element.getChild("Result", ns);

        if (temp != null) {
            result = new UnsignedByte(temp);
        }

        element.removeChild("Result", ns);
        temp = element.getChild("OpSpecID", ns);

        if (temp != null) {
            opSpecID = new UnsignedShort(temp);
        }

        element.removeChild("OpSpecID", ns);

        //parameter - not choices - no special actions needed
        temp = element.getChild("ReadDataInfo", ns);

        if (temp != null) {
            readDataInfo = new ReadDataInfo(temp);
            LOGGER.info(
                "setting parameter readDataInfo for parameter HbCustomizedReadSpecResult");
        }

        if (temp == null) {
            LOGGER.info(
                "HbCustomizedReadSpecResult misses optional parameter of type readDataInfo");
        }

        element.removeChild("ReadDataInfo", ns);

        if (element.getChildren().size() > 0) {
            String message = "HbCustomizedReadSpecResult has unknown element " +
                ((Element) element.getChildren().get(0)).getName();
            throw new InvalidLLRPMessageException(message);
        }
    }

    //setters
    /**
    * set   result of type UnsignedByte .
    * @param   result to be set
    */
    public void setResult(final UnsignedByte result) {
        this.result = result;
    }

    /**
    * set   opSpecID of type UnsignedShort .
    * @param   opSpecID to be set
    */
    public void setOpSpecID(final UnsignedShort opSpecID) {
        this.opSpecID = opSpecID;
    }

    /**
    * set readDataInfo of type ReadDataInfo.
    * @param  readDataInfo to be set
    */
    public void setReadDataInfo(final ReadDataInfo readDataInfo) {
        this.readDataInfo = readDataInfo;
    }

    // end setter

    //getters
    /**
    * get   result of type UnsignedByte.
    * @return   type UnsignedByte to be set
    */
    public UnsignedByte getResult() {
        return this.result;
    }

    /**
    * get   opSpecID of type UnsignedShort.
    * @return   type UnsignedShort to be set
    */
    public UnsignedShort getOpSpecID() {
        return this.opSpecID;
    }

    /**
    * get readDataInfo of type ReadDataInfo .
    * @return  ReadDataInfo
    */
    public ReadDataInfo getReadDataInfo() {
        return readDataInfo;
    }

    // end getters

    //add methods

    // end add

    /**
    * For TLV Parameter length can not be determined at compile time. This method therefore always returns 0.
    * @return Integer always zero
    */
    public static Integer length() {
        return 0;
    }

    /**
    * {@inheritDoc}
    */
    public SignedShort getTypeNum() {
        return TYPENUM;
    }

    /**
    * {@inheritDoc}
    */
    public String getName() {
        return "HbCustomizedReadSpecResult";
    }

    /**
    * return string representation. All field values but no parameters are included
    * @return String
    */
    public String toString() {
        String result = "HbCustomizedReadSpecResult: ";
        result += ", result: ";
        result += result;
        result += ", opSpecID: ";
        result += opSpecID;
        result = result.replaceFirst(", ", "");

        return result;
    }
}
