/*
 *
 * This file was generated by LLRP Code Generator
 * see http://llrp-toolkit.cvs.sourceforge.net/llrp-toolkit
 * for more information
 * Generated on: Mon Apr 10 16:16:15 CST 2017;
 *
 */

/*
 * Copyright 2007 ETH Zurich
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 *
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions
 * and limitations under the License.
 *
 */
package org.llrp.ltk.generated.parameters;

import org.apache.log4j.Logger;

import org.jdom.Content;
import org.jdom.Document;
import org.jdom.Element;
import org.jdom.Namespace;

import org.llrp.ltk.exceptions.InvalidLLRPMessageException;
import org.llrp.ltk.exceptions.MissingParameterException;
import org.llrp.ltk.generated.LLRPConstants;
import org.llrp.ltk.types.LLRPBitList;
import org.llrp.ltk.types.LLRPMessage;
import org.llrp.ltk.types.SignedShort;
import org.llrp.ltk.types.TLVParameter;
import org.llrp.ltk.types.TVParameter;
import org.llrp.ltk.types.UTF8String_UTF_8;
import org.llrp.ltk.types.UnsignedInteger;
import org.llrp.ltk.types.UnsignedShort;

import java.util.LinkedList;
import java.util.List;


/**
 *

See also

 */

/**
 *

See also
      .
 */
public class Status extends TLVParameter {
    public static final SignedShort TYPENUM = new SignedShort(300);
    private static final Logger LOGGER = Logger.getLogger(Status.class);
    protected UnsignedInteger statusCode;
    protected UTF8String_UTF_8 errorDescription;

    /**
     * empty constructor to create new parameter.
     */
    public Status() {
    }

    /**
     * Constructor to create parameter from binary encoded parameter
     * calls decodeBinary to decode parameter.
     * @param list to be decoded
     */
    public Status(LLRPBitList list) {
        decodeBinary(list);
    }

    /**
    * Constructor to create parameter from xml encoded parameter
    * calls decodeXML to decode parameter.
    * @param element to be decoded
    */
    public Status(Element element) throws InvalidLLRPMessageException {
        decodeXML(element);
    }

    /**
    * {@inheritDoc}
    */
    public LLRPBitList encodeBinarySpecific() {
        LLRPBitList resultBits = new LLRPBitList();

        if (statusCode == null) {
            LOGGER.warn(" statusCode not set");
            throw new MissingParameterException(
                " statusCode not set  for Parameter of Type Status");
        }

        resultBits.append(statusCode.encodeBinary());

        if (errorDescription == null) {
            LOGGER.warn(" errorDescription not set");
            throw new MissingParameterException(
                " errorDescription not set  for Parameter of Type Status");
        }

        resultBits.append(errorDescription.encodeBinary());

        return resultBits;
    }

    /**
    * {@inheritDoc}
    */
    public Content encodeXML(String name, Namespace ns) {
        // element in namespace defined by parent element
        Element element = new Element(name, ns);
        // child element are always in default LLRP namespace
        ns = Namespace.getNamespace("", LLRPConstants.LLRPNAMESPACE);

        if (statusCode == null) {
            LOGGER.warn(" statusCode not set");
            throw new MissingParameterException(" statusCode not set");
        } else {
            element.addContent(statusCode.encodeXML("StatusCode", ns));
        }

        if (errorDescription == null) {
            LOGGER.warn(" errorDescription not set");
            throw new MissingParameterException(" errorDescription not set");
        } else {
            element.addContent(errorDescription.encodeXML("ErrorDescription", ns));
        }

        //parameters
        return element;
    }

    /**
    * {@inheritDoc}
    */
    protected void decodeBinarySpecific(LLRPBitList binary) {
        int position = 0 + TYPENUMBERLENGTH + PARAMETERTYPELENGTH; //change by wuwh 
        int tempByteLength;
        int tempLength = 0;
        int count;
        SignedShort type;
        int fieldCount;
        statusCode = new UnsignedInteger(binary.subList(position,
                    UnsignedInteger.length()));
        position += UnsignedInteger.length();
        // array. first 16 bits indicate length of array
        fieldCount = new UnsignedShort(binary.subList(position,
                    UnsignedShort.length())).toShort();
        tempLength = (UTF8String_UTF_8.length() * fieldCount) +
            UnsignedShort.length();
        errorDescription = new UTF8String_UTF_8(binary.subList(position,
                    tempLength));
        position += tempLength;
        LOGGER.debug("decoding array of type: UTF8String_UTF_8 with " +
            tempLength + " length");

        //might need padding
        // must always be blocks of 8 bites, if it is a bitlist, this might not be automatically the case
        if ((tempLength % 8) > 0) {
            position += (8 - (tempLength % 8));
            LOGGER.info("padding needed for errorDescription ");
        }
    }

    /**
    * {@inheritDoc}
    */
    public void decodeXML(Element element) throws InvalidLLRPMessageException {
        List<Element> tempList = null;
        boolean atLeastOnce = false;

        Element temp = null;

        // child element are always in default LLRP namespace
        Namespace ns = Namespace.getNamespace(LLRPConstants.LLRPNAMESPACE);

        temp = element.getChild("StatusCode", ns);

        if (temp != null) {
            statusCode = new UnsignedInteger(temp);
        }

        element.removeChild("StatusCode", ns);
        temp = element.getChild("ErrorDescription", ns);

        if (temp != null) {
            errorDescription = new UTF8String_UTF_8(temp);
        }

        element.removeChild("ErrorDescription", ns);

        if (element.getChildren().size() > 0) {
            String message = "Status has unknown element " +
                ((Element) element.getChildren().get(0)).getName();
            throw new InvalidLLRPMessageException(message);
        }
    }

    //setters
    /**
    * set   statusCode of type UnsignedInteger .
    * @param   statusCode to be set
    */
    public void setStatusCode(final UnsignedInteger statusCode) {
        this.statusCode = statusCode;
    }

    /**
    * set errorDescription of type UTF8String_UTF_8 .
    * @param  errorDescription to be set
    */
    public void setErrorDescription(final UTF8String_UTF_8 errorDescription) {
        this.errorDescription = errorDescription;
    }

    // end setter

    //getters
    /**
    * get   statusCode of type UnsignedInteger.
    * @return   type UnsignedInteger to be set
    */
    public UnsignedInteger getStatusCode() {
        return this.statusCode;
    }

    /**
    * get errorDescription of type  UTF8String_UTF_8.
    * @return  UTF8String_UTF_8
    */
    public UTF8String_UTF_8 getErrorDescription() {
        return errorDescription;
    }

    // end getters

    //add methods

    // end add

    /**
    * For TLV Parameter length can not be determined at compile time. This method therefore always returns 0.
    * @return Integer always zero
    */
    public static Integer length() {
        return 0;
    }

    /**
    * {@inheritDoc}
    */
    public SignedShort getTypeNum() {
        return TYPENUM;
    }

    /**
    * {@inheritDoc}
    */
    public String getName() {
        return "Status";
    }

    /**
    * return string representation. All field values but no parameters are included
    * @return String
    */
    public String toString() {
        String result = "Status: ";
        result += ", statusCode: ";
        result += statusCode;
        result += ", errorDescription: ";
        result += errorDescription;
        result = result.replaceFirst(", ", "");

        return result;
    }
}
